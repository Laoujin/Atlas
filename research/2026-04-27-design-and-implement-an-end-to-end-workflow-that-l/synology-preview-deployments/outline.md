What is the standard pattern for per-PR preview deployments and why teams adopt them?
What Synology DSM 7.x features (Container Manager, reverse proxy, Web Station, Task Scheduler) underpin self-hosted preview deployments?
How do you configure wildcard DNS + a Let's Encrypt wildcard cert on Synology so each PR gets its own *.preview.example.com URL?
What are the practical CI/CD integration patterns to push a per-PR build from GitHub Actions to a Synology host (SSH/rsync vs. registry-pull vs. webhook/agent)?
Container orchestration choices on Synology for ephemeral preview envs: Container Manager + Compose vs. Portainer vs. Dokploy vs. Coolify vs. k3s — which fits, which doesn't?
Real-world Synology preview-deployment setups, gotchas and limitations from r/homelab, r/synology, and homelab blog write-ups?