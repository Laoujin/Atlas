What is the minimum reference Dockerfile for a Claude Code YOLO container in 2026 — Python/uv, Node/nvm, .NET SDK, Playwright with browsers+deps, gh CLI, git, ripgrep, jq, ffmpeg/imagemagick, poppler/tesseract/pandoc, LibreOffice, and Python libs (openpyxl, python-docx, python-pptx, Pillow, opencv)?
How does Claude Code discover and load skills on disk, and how do people preinstall or mount skill packs like obra/superpowers and Office document skills inside a container?
What are the recommended ways to persist authenticated sessions (Claude Code login, gh auth) across container restarts without leaking secrets — named volumes, bind-mounted ~/.claude and ~/.config/gh, Docker secrets, host credential helpers, per-project tokens — and why are raw env-var API keys the worst option?
What sandbox-hardening controls actually stop a malicious prompt from exfiltrating data or escaping — deny-by-default egress with proxy allowlist, read-only rootfs, dropped capabilities, seccomp/AppArmor, no Docker socket, non-root user, separate workspace volume, blocked metadata endpoints?
How do the main community approaches (HolyClaude, Anthropic's devcontainer, claude-code-sandbox, 2026 alternatives like sculptor/container-use/agent-sandbox) compare on setup effort, isolation strength, tool coverage, auth UX, and maintenance cost?
Is YOLO-inside-a-sandbox actually safer than native Claude Code with per-action prompts — what's the 2026 consensus on prompt-injection threats, real incidents, and the tradeoffs being debated?
